services:
  github-proxy-test:
    container_name: github-proxy
    image: nginx:1.27.4-alpine-amd64
    ports:
      - 48080:80
    volumes:
      - ./src/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./src:/app
