services:
  github-proxy-test:
    container_name: github-proxy
    image: nginx:1.31.1-alpine
    ports:
      - 48080:80
    volumes:
      - ./src/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./src:/app
